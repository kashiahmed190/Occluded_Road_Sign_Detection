# Training — legacy cropped-patch pipeline (superseded)

Trained on 1244 pre-cropped single-sign patches instead of the 877 full
images. Kept for reproducibility of earlier results. See the root README
for why the `fullimage/` pipeline is recommended instead.
